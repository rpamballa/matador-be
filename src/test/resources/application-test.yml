spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

sentry:
  dsn:

matador:
  # Empty external keys -> mock identity provider, ephemeral JWT keypair.
  stripe:
    secret-key:
    identity-webhook-secret:

logging:
  level:
    com.matador: INFO
