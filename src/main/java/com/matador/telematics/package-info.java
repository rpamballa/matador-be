/**
 * Telematics module: an abstraction over vehicle telematics providers (Smartcar, Tesla,
 * Mock). Implements the vehicle module's command port and polls vehicle snapshots.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Telematics")
package com.matador.telematics;
